global:
  scrape_interval: 30s
  evaluation_interval: 30s
  external_labels:
    env: production

scrape_configs:
  - job_name: hallyuhub
    static_configs:
      - targets: ['hallyuhub:3000']
    metrics_path: /api/metrics
    scheme: http
