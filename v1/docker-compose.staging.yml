version: '3.8'

services:
  hallyuhub-staging:
    image: ghcr.io/uchidate/khub:latest
    container_name: hallyuhub-staging
    ports:
      - "3001:3000"
    env_file:
      - .env.staging
    volumes:
      - hallyuhub-data:/app/data
    networks:
      - web
    restart: always

networks:
  web:
    external: true

volumes:
  hallyuhub-data:
    external: true
